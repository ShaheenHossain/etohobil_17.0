odoo.define('etohobil.sync_amount', function (require) {
    "use strict";

    var FormController = require('web.FormController');
    var rpc = require('web.rpc');

    FormController.include({
        // Your custom logic here
    });
});
